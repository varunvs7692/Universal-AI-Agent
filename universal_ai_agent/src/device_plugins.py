def send_sms(phone_number, message):
    # Simulate sending an SMS (replace with real integration as needed)
    print(f"[Device] SMS sent to {phone_number}: {message}")
    return f"SMS sent to {phone_number}: {message}"
